#include <iostream>
#include <vector>   // vector is a container
using namespace std;

int main(){
  // int N;
  // // int a[N];
  // cin >> N;
  // vector<int> v(N, 5);
  // for (int i=0; i<N; i++){
  //   cout << v[i] << " ";
  // }
  // cout << endl;
  vector<int> v;
  v.push_back(5);
  v.push_back(6);
}